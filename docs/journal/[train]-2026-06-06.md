# [Train] 2026-06-06 - Chuyen NACIR thanh clean-safe main method

> **Ngay:** 2026-06-06
> **Pham vi:** Training / loss / data / robustness
> **Muc tieu:** Bien NACIR thanh framework chinh co clean fallback gan Circle va chuan bi benchmark synthetic FP/FN noise.
> **Trang thai cuoi ngay:** FOLLOW-UP

## 1. Tom tat quyet dinh

- VN3K da duoc confirm la clean, nen NACIR khong nen duoc ky vong tang clean result bang cach "sua noise" tren tap nay.
- Huong moi: trinh bay NACIR la **noise-aware framework with clean fallback**, trong do Circle Loss la special case khi detector khong co bang chung noise du manh.
- Muc tieu rerun clean cho `run_nacir.sh`: `test_t2i_R1 >= 51.8`.
- Khong claim aggressive detector-on NACIR cu dat `52.28%`. Can rerun clean-safe NACIR truoc khi dung NACIR lam main method trong bao cao/do an.
- Robustness se duoc chung minh bang synthetic FP, synthetic FN, va FP+FN noise.

## 2. Boi canh

- Ket qua NACIR clean truoc do bi giam ve khoang `50.70`, kha nang cao vi FN detector suppress nham hard negatives, lam yeu hard-negative mining cua Circle Loss.
- Detector-off run dat khoang `51.65`, gan hon voi baseline, cho thay phan Circle/clean path van on hon aggressive FN branch.
- Vi VN3K clean, FN branch tren clean phai gan nhu no-op; detector chi nen can thiep khi posterior FN rat cao va similarity nam sat vung positive.

## 3. Thi nghiem / thay doi da lam

| Hang muc | Noi dung |
|---|---|
| Conservative FN gate | Them gate trong `compute_noise_aware_circle()` de chi suppress negative khi `P(FN) >= 0.80`, `s_n >= mu_pos - 1.0 * sigma_pos`, `mu_pos > mu_neg`, va so negative bi suppress khong vuot `5%`. |
| Diagnostics | Them log `nacir_fn_selected_frac`, `nacir_fn_gate_active`, `nacir_fn_prob_max`, `nacir_fn_prob_selected_mean`; giu cac metric cu nhu `nacir_alpha_n_scale_mean`, `nacir_clean_weight_mean`, `gmm_separation`, `gmm_fallback`. |
| Clean-safe config | Cap nhat NACIR defaults: `fn_prior=0.003`, `epsilon_n=0.85`, `fn_safe_gate=true`, `fn_prob_threshold=0.80`, `fn_pos_sigma_k=1.0`, `fn_max_suppress_frac=0.05`, `fn_enable_epoch=21`. |
| Main script | Cap nhat `run_nacir.sh` thanh clean-safe main method, khong con override aggressive `fn_prior=0.010`, `epsilon_n=0.60`; van nhan Hydra override tu CLI. |
| Synthetic FN injection | Them `dataset.fn_noisy_rate` va `dataset.fn_noisy_file`; train set co the doi mot phan PID thanh fake PID moi de tao false-negative labels, trong khi image/text/caption va val/test giu clean. |
| Dataset config | Them default `fn_noisy_rate: 0.0` va `fn_noisy_file: null` cho cac dataset config hien co. |
| Tests | Them unit tests cho clean-safe NACIR objective va FN injection. |

## 4. Ket qua

| Kiem tra | Lenh | Ket qua |
|---|---|---|
| NACIR clean-safe objective | `venv/bin/python -m unittest tests/test_nacir_clean_safe.py` | `Ran 5 tests ... OK` |
| FN noise injection | `venv/bin/python -m unittest tests/test_noise_injection.py` | `Ran 4 tests ... OK` |
| Test discovery | `venv/bin/python -m unittest discover tests` | `Ran 9 tests ... OK` |
| Compile check | `venv/bin/python -m compileall src/msiglip/model/objectives.py src/msiglip/model/noise_aware.py src/msiglip/model/tbps.py src/msiglip/data/bases.py src/msiglip/lightning_data.py tests/test_nacir_clean_safe.py tests/test_noise_injection.py` | OK |
| Whitespace check | `git diff --check` | OK |
| Clean full training | `bash run_nacir.sh` | Chua chay |
| Robustness FP/FN training | Circle vs NACIR tren FP/FN/FP+FN noise | Chua chay |

Ghi chu: `uv run python -m unittest ...` bi chan tren may local vi `bitsandbytes==0.45.5` khong co wheel cho macOS arm64, nen da dung `venv/bin/python` de verify logic.

## 5. Dien giai

- Thay doi lan nay khong chi la config; FN branch duoc sua o muc objective de clean data mac dinh gan nhu giu nguyen Circle hard-negative mining.
- Neu clean data that su khong nhieu, `nacir_fn_selected_frac` nen rat thap va `nacir_alpha_n_scale_mean` nen gan `1.0`.
- Synthetic FN injection giup test dung diem yeu can chung minh: khi label PID bi tach sai thanh nhieu identity gia, NACIR co nen robust hon Circle hay khong.
- Ket luan "NACIR la main method" van chua duoc khoa lai cho toi khi full clean run dat nguong va robustness proof co ket qua.

## 6. Quyet dinh tiep theo

- [ ] Chay clean acceptance run:

```bash
bash run_nacir.sh
```

Pass neu `test_t2i_R1 >= 51.8`.

- [ ] Neu clean run fail, doc diagnostics:
  - `nacir_fn_selected_frac > 0.05`: gate van qua manh hoac cap dang khong hieu qua.
  - `nacir_alpha_n_scale_mean < 0.98`: FN suppression van lam yeu qua nhieu negative mining.
  - `nacir_fn_gate_active` bat qua som/qua nhieu: can tang threshold hoac delay `fn_enable_epoch`.

- [ ] Chay robustness proof toi thieu voi FP noise `0.4`: Circle vs NACIR.

```bash
bash run_nacir.sh dataset.noisy_rate=0.4 dataset.noisy_file=artifacts/training/noiseindex/VN3K_VI_0.4.npy
```

- [ ] Chay robustness proof toi thieu voi FN noise `0.4`: Circle vs NACIR.

```bash
bash run_nacir.sh dataset.fn_noisy_rate=0.4 dataset.fn_noisy_file=artifacts/training/noiseindex/VN3K_VI_FN_0.4.npy
```

- [ ] Chay robustness proof toi thieu voi FP+FN noise `0.4`: Circle vs NACIR.

```bash
bash run_nacir.sh dataset.noisy_rate=0.4 dataset.noisy_file=artifacts/training/noiseindex/VN3K_VI_0.4.npy dataset.fn_noisy_rate=0.4 dataset.fn_noisy_file=artifacts/training/noiseindex/VN3K_VI_FN_0.4.npy
```

- [ ] Neu NACIR giam it hon Circle o it nhat `2/3` scenario va khong collapse o scenario con lai, mo rong curve voi rates `0.2` va `0.6`.
- [ ] Sau khi co server logs, append ket qua vao journal dung ngay chay experiment, khong ghi vao `docs/knowledge.md`.

## 7. Cau hoi mo

- Clean-safe NACIR co dat `test_t2i_R1 >= 51.8` voi seed hien tai khong?
- Conservative FN gate co qua bao thu tren synthetic FN noise khong?
- Neu clean-safe NACIR chi dat sat nguong, co can chay same-seed Circle control voi code hien tai de so sanh cong bang khong?
- Khi robustness proof pass, nen trinh bay NACIR la "main method" theo huong framework, hay la extension tren Circle Loss trong phan ablation?
