# [Deploy] 2026-06-15 - v68 chặn A16 diện rộng → chọn rotation-based INT8 (QuaRot/SliceGPT)

> **Ngày:** 2026-06-15
> **Thiết bị / Runtime:** RB3 Gen2 / QNN HTP **V68** / AI Hub
> **Model / Artifact nguồn:** `exported_model_gelu20` (opset-20 fused Gelu, ≡ baseline 52.28)
> **Mục tiêu:** Tìm cấu hình INT8 deploy được trên HTP **v68** mà vẫn giữ fidelity
> **Plan checklist:** `deployment/docs/journal/[deploy-plan]-2026-06-14.md`
> **Trạng thái cuối ngày:** FOLLOW-UP - chọn hướng rotation, bắt đầu implement

---

## 1. Bối cảnh

- 2026-06-14: `gelu20 + W8A16` đạt QDQ `0.99969` (fidelity PASS) nhưng **link fail trên v68**: attention matmul (act×act) A16 cần arch **v73+** (job `j576q80rg`, op `node_MatMul_774`).

## 2. Job / kết quả hôm nay

| Job | Loại | Kết quả |
|---|---|---|
| `jp48z6lqg` | compile mixed-int (attention int8 + rest int16) | SUCCESS |
| `jpxmw8kjg` | link mixed-int | **FAILED** exit 14, op `node_layer_norm`, "Value 68, expected >= 73" |

- Đã tự code: extend `retarget_qdq_dtype.py` thêm `--act-matmul-inputs` → hạ attention matmul (act×act) xuống int8 (56 QDQ pairs), giữ residual/MLP int16. Local fidelity `0.9943` (PASS).
- Nhưng link vẫn fail ở **LayerNorm** (op khác): v68 cũng không hỗ trợ **A16 cho LayerNorm**. → giới hạn A16 trên v68 là **diện rộng** (matmul act×act, LayerNorm, ...), không phải vài op lẻ.

## 3. Thử clip (percentile) cho W8A8 - FAIL

- `tune_qdq_activation_encodings.py --all-activations --max-abs {4,8,16,32,64}` trên gelu20 W8A8 (job `jgj1jqm8g`):
  - cosine: `0.118 / 0.155 / 0.128 / 0.405 / 0.190` — tất cả fail xa gate.
- Kết luận: **outlier channels mang thông tin thật**, không thể clip bỏ. Per-tensor INT8 phải BẢO TOÀN thông tin outlier.

## 4. Chẩn đoán hợp nhất (khóa)

- v68: hỗ trợ **INT8 (W8A8) diện rộng**; **A16 chỉ cho act×weight** (projection/conv), KHÔNG cho act×act (attention) và LayerNorm (cần v73+).
- Model cần A16 ở **residual + LayerNorm** (concentration). Mâu thuẫn: chỗ cần A16 đúng là chỗ v68 từ chối A16.
- Clip outlier → fail (outlier mang tin). FP16-on-HTP chạy được nhưng chậm/nặng.
- **Lối ra INT8 duy nhất: rotation-based equalization** — xoay residual stream bằng ma trận trực giao để **rải đều** outlier ra mọi kênh (không cắt, không cần 16-bit) → per-tensor INT8 đủ → all-INT8 → v68 OK. Output-invariant (gập xoay vào weights).

## 5. Quyết định: implement rotation (QuaRot/SliceGPT-style) trên PyTorch

User còn ~1 tháng → chọn hướng này (thay vì FP16). Làm trên PyTorch (truy cập module theo tên), rồi export opset-20 → quantize W8A8.

### Cấu trúc model (đã inspect)

- `backbone.vision_model` = SiglipVisionTransformer: `embeddings` (patch_embedding Conv2d + position_embedding) → `encoder.layers.0..11` (mỗi layer: `layer_norm1`→`self_attn`(q/k/v/o proj)→residual, `layer_norm2`→`mlp`(fc1/gelu/fc2)→residual) → `post_layernorm` → `head` (MultiheadAttentionPoolingHead: attention + layernorm + mlp).
- LayerNorm đều affine, shape (768,). Residual dim = 768.
- LƯU Ý: TBPS có cả `backbone.vision_model` lẫn top-level `vision_model`; cần xác nhận `encode_image` dùng cái nào trước khi sửa.

### Công thức (recipe)

**Phase A — LayerNorm → RMSNorm (output-invariant):**
1. Với mỗi LN(γ,β) feeding consumer linear W(+b): fold γ vào W (`W·diag(γ)`), fold β vào bias (`b + W·β`); set LN affine = identity.
2. Làm residual mean-zero: fold `M = I − (1/d)·11ᵀ` vào MỌI weight ghi vào residual (embeddings out, mỗi o_proj, mỗi fc2): `W_out → M·W_out`, `b_out → M·b_out`. Vì LN trừ mean nên `LN(Mx)=LN(x)` → bất biến. Sau bước này LN = RMSNorm.

**Phase B — rotate residual bằng Q trực giao (768×768):**
- Writers (embeddings out, o_proj, fc2): `W_out → Q·W_out`, `b_out → Q·b_out`.
- Readers (q/k/v/fc1 qua LN đã fold γ; head K/V qua post_layernorm): `W_in → W_in·Qᵀ`.
- RMSNorm(Qx)=Q·RMSNorm(x) nên xoay đi xuyên norm. Q dùng random orthogonal (offline fold, không cần fast Hadamard; không thêm op runtime).
- Head: chỉ cần fold Qᵀ vào K/V proj của `head.attention` (probe/query là learned, không đổi). Có thể localize rotation trong encoder rồi để post_layernorm+head tự nhận lại nhờ fold Qᵀ.

### Milestones

- **M1**: Phase A + B, FP32 `encode_image` cosine ≈ `1.0` (output-invariant). [local, free]
- **M2**: export opset-20 (fused Gelu) bản rotated, static control ≈ `1.0`. [local]
- **M3**: AI Hub quantize **W8A8** + compare ≥ `0.95`. [1 job]
- **M4**: compile/link all-int8 → v68 link SUCCESS → `.bin`.
- **M5**: `qnn-net-run` board + latency/RAM; replicate text.

### Không làm

- Không compile/link thêm W8A16/mixed-A16 trên v68 (đã chứng minh chặn diện rộng).
- Không clip outlier (đã fail).
- Mọi bước tới M3 test local trước khi tốn job AI Hub.

## 6. Rủi ro

- LayerNorm fusion (mean-subtraction + affine) là phần dễ sai; dùng output-invariance làm safety net.
- Head (nn.MultiheadAttention, packed in_proj) fiddly khi fold Qᵀ.
- ~~Cần xác nhận `encode_image` path~~ ĐÃ RÕ: `encode_image` dùng **top-level `self.vision_model`** (SiglipVisionTransformer), lấy `.pooler_output`, rồi nếu `backbone` có `visual_projection` thì `pooler_output = backbone.visual_projection(pooler_output)`. → Transform áp lên `model.vision_model` (KHÔNG phải `backbone.vision_model`). Nếu localize rotation trong encoder + restore tại post_layernorm/head thì `pooler_output` không đổi → `visual_projection` không cần đụng.
- M1 implementation là build lớn, nên bắt đầu với context tươi; recipe + path đã ghi đầy đủ ở mục 5 để tiếp tục sạch.

---

## 7. M1 DONE + reload bug đã fix (review)

- Script `deployment/scripts/qnn/rotate_vision_encoder.py`. Gate in-memory: encode_image FP32 cosine `1.0` (min `0.99999988`). Concentration residual `220x → 4.2x`, abs-max `267.6 → 140.2`. Logic fold (affine, mean-subtraction M, Q/Qᵀ, head K/V) đã review = đúng.
- **BUG (review bắt được):** `_save_rotated` chỉ lưu state_dict; RMSNormNoAffine paramless nên reload bằng `_load_pytorch_model` (dựng lại LayerNorm trừ mean) làm hỏng residual đã xoay (non-mean-zero) → reload cosine **`0.11`**. Gate in-memory không bắt vì chạy trên object đã transform trong RAM.
- **FIX:** thêm `load_rotated_model(model_dir)` vào `rotate_vision_encoder.py` — dựng model → swap LN1/LN2/post_layernorm sang `RMSNormNoAffine` → load state_dict. Verify: reload cosine về **`1.0`** (min `0.99999988`).
- **M2 BẮT BUỘC:** mọi nơi load model rotated (export ONNX, compare) phải dùng `load_rotated_model`, KHÔNG dùng `_load_pytorch_model` trực tiếp.
- **Cảnh báo M2/M3:** LN→RMSNorm đổi op export. RMSNorm có `x.pow(2)` → opset-20 export thành ops rời (không phải `LayerNormalization` fused). Cần kiểm static-control ≈ 1.0 và xem `Pow(x,2)`/range có gây vấn đề quantize trên v68 không (bài học từ `Pow(x,3)` GELU). Nếu RMSNorm quantize xấu, cân nhắc giữ op RMSNorm fused (opset 23+) hoặc xử lý riêng.

---

## 8. M2 DONE + review (export ONNX rotated)

- Script mới `deployment/scripts/qnn/export_rotated_vision_onnx.py`; `compare_onnx_with_pytorch.py` thêm `--rotated` (dùng `load_rotated_model`); `profile_activation_outliers.py` thêm fallback dò block qua `q_proj.bias` cho model rotated (LN đã thành RMSNorm, mất initializer `layer_norm1.weight`).
- Output: `artifacts/deployment/exports/exported_model_rotated/vision_onnx/{vision_encoder.onnx,.data}`.
- **GATE M2 PASS (verify độc lập):** static control ONNX-rotated vs PyTorch-rotated `cosine_l2_mean 1.0000000`, `min 0.99999988`, no NaN. (Full-chain vs gốc cũng `1.0`.)
- Op counts: `Gelu=13` (fused, giữ bài học cubic), `LayerNormalization=1` (chỉ head.layernorm), `Pow=25 + ReduceMean=25` (RMSNorm decomposed).
- Concentration residual (ONNX, blocks 4-11): `220x → ~10x` (DIFFUSE). LƯU Ý đọc số concentration, KHÔNG đọc text verdict (heuristic thiết kế trước rotation, gán "DIFFUSE→W8A16" không áp dụng). Dòng 2490x ở head là artifact (abs-max 0.5, median≈0).
- **Review RMSNorm Pow(x²) (rủi ro M1 flag):** đo độc lập → concentration chỉ **18.9x** (worst), median 17.5x, abs-max thực ~941 (không ~19600). + feed `ReduceMean` (trung bình 768 kênh) → nhiễu quantize bị giảm ~√768. → **rủi ro M3 thấp**, không phải bottleneck.

### M3 (xanh đèn): quantize W8A8 trên rotated, gate ≥ 0.95

- Đây là **job AI Hub đầu tiên** của hướng rotation → log job vào journal theo rule.
- Compare QDQ vs PyTorch GỐC (`--model-dir exported_model`, KHÔNG `--rotated`) vì rotated FP32 ≡ gốc; đo tổng sai số quantize.
- Kỳ vọng: W8A8 giờ pass (residual không còn 1 kênh áp đảo). Nếu pass → M4 compile/link all-int8 → **v68 OK** (int8 diện rộng được hỗ trợ).

---

## 9. M3 FAIL (`0.16`) → root cause RMSNorm + FIX (mean-preserving rotation)

- M3 W8A8 trên rotated (job `jp48z6o2g`): compare vs PyTorch gốc = `cosine_l2_mean 0.162` — gần như không hơn W8A8 chưa xoay (`0.14`).
- Surgery decompose: `all_activations_float = 0.978` (weights OK), `all_weights_float = 0.162` → **lỗi 100% ở ACTIVATION** dù residual concentration đã `220x→10x`.
- Đo trực tiếp QDQ: top activation tensors theo range đều là **`Pow` output** (RMSNorm x²), abs_max tới 918; top-30 toàn `Pow/ReduceMean/Add`.
- **ROOT CAUSE:** đổi LayerNorm → RMSNorm decomposed đã **phơi bày nội bộ normalization (`Pow/ReduceMean/Div`) ra cho per-tensor quant.** Model gốc dùng **LayerNorm FUSED** (HTP tính nội bộ, chỉ quantize I/O). Rotation làm đúng (residual diffuse) nhưng tác dụng phụ "phải RMSNorm" tạo bệnh mới. (Lập luận "ReduceMean trung bình hóa nên ổn" ở M2 SAI.)

### FIX: mean-preserving rotation → giữ LayerNorm fused

- Dùng **Q bảo toàn mean** (`Q·1 = 1`): xoay random trong không gian con trực giao với vector `1`, giữ nguyên chiều mean. Khi đó `mean(Qx)=mean(x)`, `std(Qx)=std(x)` → **identity-affine LayerNorm commute với Q** → KHÔNG cần RMSNorm.
- Sửa `rotate_vision_encoder.py`:
  - `_mean_preserving_orthogonal(dim,seed)`: `Q = U·blockdiag(1,Rc)·Uᵀ`, `U[:,0]=1/√d`.
  - `phase_a`: fold γ/β vào readers + `_set_ln_identity` (giữ LayerNorm op, KHÔNG replace RMSNorm, KHÔNG fold M).
  - `phase_b`: dùng Q mean-preserving.
  - `load_rotated_model`: đơn giản lại (LayerNorm chuẩn → `_load_pytorch_model` load đúng; bỏ swap RMSNorm).
- **Validate local (đã chạy):** invariance Phase A & B `1.0` (min `0.99999988`); Q orthogonal `3e-15`, `Q·1=1` err `1e-14`; reload `1.0`; **residual concentration `252x → 5.3x`** (tốt hơn cả lần RMSNorm). Đã ghi đè `exported_model_rotated` bằng bản đúng.

### Việc cần làm lại (user)

- **M2 lại:** export `exported_model_rotated` → ONNX opset-20. Kiểm op counts: phải có **`LayerNormalization` (~25-26 op), `Pow=0`** (RMSNorm/x² biến mất), `Gelu=13`. Static control ≈ `1.0`.
- **M3 lại:** W8A8 quantize + compare. Kỳ vọng giờ PASS (không còn Pow phơi ra; mọi activation quantized đều diffuse ~5x).

---

## 10. M2 + M3 chạy lại (bản fix) — `0.14 → 0.90`, near-pass

### M2 (export bản fix)

- Re-export `exported_model_rotated` → ONNX opset-20. **Op counts: `LayerNormalization=26, Pow=0, Gelu=13, ReduceMean=0`** → xác nhận giữ LayerNorm fused (RMSNorm/x² đã biến mất). Static control ≈ `1.0`.

### M3 (W8A8 trên bản fix)

- Job quantize-only `jpr9zro9p` (W8A8, calib `d7jzjy1m2`). Compare vs PyTorch gốc (`vn3k_test_10`):
  - **`cosine_l2_mean = 0.8975`, `min = 0.8747`, `max = 0.9297`**, no NaN.
- Hành trình W8A8: `0.14` (thô) → `0.16` (rotation+RMSNorm, sai) → **`0.90`** (rotation+LayerNorm fused, đúng).

### Chẩn đoán phần còn thiếu (`0.90` vs gate `0.95`)

- Top activation range trong QDQ giờ chỉ `~64` (Cast = hằng số `head_dim`, vô hại), `41` (Gelu), `39` (fc1 Add) — **KHÔNG còn outlier 918** như bản RMSNorm.
- Lỗi còn lại **phân tán** (Add 19, Gelu 6, MatMul 3 trong top-40), không phải 1 bottleneck → là **sai số INT8 per-tensor "bình thường" tích lũy** qua 12 block. Concentration đã được rotation giải xong.

### Quyết định: đo R@1 thật thay vì vắt thêm cosine

- Gate `0.95` là proxy bảo thủ chưa hiệu chỉnh (user đã đặt nghi vấn trước đó). Model giờ **all-INT8** (không A16, không Pow) → **deploy được trên v68**.
- **M4: compile/link** (kỳ vọng link SUCCESS vì all-int8 v68 hỗ trợ) → binary deploy đầu tiên trên NPU.
- Sau đó **đo R@1 thật**; nếu `≥ 48` → ship, proxy đã xong việc.
- Chỉ nếu R@1 chưa đủ mới đầu tư **QuaRot R2-R4** (xoay thêm attention + MLP down-proj) đẩy `0.90→0.95`.

### Lệnh M4 (đang chờ)

```bash
python3 deployment/scripts/qnn/submit_qaihub_quantize_compile.py --model artifacts/deployment/exports/exported_model_rotated/vision_onnx --calibration-data d7jzjy1m2 --weights-dtype int8 --activations-dtype int8 --wait --download artifacts/deployment/runtime/rotated_w8a8_v2/vision_encoder.bin
```

- Điểm cần xác nhận: **link có SUCCESS trên v68 không** — đây là mục tiêu cốt lõi của cả hướng rotation (all-int8 không vướng giới hạn A16 của v68).

---

## 11. ✅✅ M4 LINK SUCCESS trên v68 — binary deploy đầu tiên

| Job | Loại | Kết quả |
|---|---|---|
| `jpv4j8lkp` | quantize W8A8 (full flow) | SUCCESS |
| `jpxmwq8lg` | compile (DLC) | SUCCESS |
| `jp2j211q5` | **link (context binary)** | **✅ SUCCESS** |

- Output: `artifacts/deployment/runtime/rotated_w8a8_v2/vision_encoder.bin` (**89.7 MB**, device "Dragonwing RB3 Gen 2 Vision Kit", compile_options `--quantize_io`).
- **Cột mốc:** lần đầu tiên model mSigLIP vision link thành công thành QNN context binary trên HTP **v68**. Giải quyết toàn bộ chuỗi link fail lịch sử (internal float → fixed bởi GELU fusion + all-int8; A16-cần-v73 → tránh bằng W8A8; concentration sụp → tránh bằng rotation).
- Công thức cuối: **opset-20 fused Gelu + mean-preserving rotation (giữ LayerNorm fused) + W8A8**. QDQ fidelity `0.90` (near-pass proxy).
- Size: 89.7MB (INT8) << FP16 ~178MB << FP32 ~356MB.

### M5 (tiếp theo): chạy board + đo thật

- Copy `.bin` + `vn3k_test_10/` sang board → `qnn-net-run` (HTP) → `compare_qnn_with_pytorch` (gate QNN vs PyTorch `≥ 0.90`).
- Đo **latency + RAM** thật trên HTP.
- Đo **R@1 thật** — con số quyết định thay proxy cosine. Nếu `≥ 48` → ship.
- Lưu ý: compile dùng `--quantize_io` → kiểm dtype I/O khi chạy `qnn-net-run`; output `.raw` mặc định được dequantize về float.
