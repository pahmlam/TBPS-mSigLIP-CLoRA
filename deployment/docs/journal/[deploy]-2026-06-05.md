# [Deploy] 2026-06-05 - Tiếp tục tìm cấu hình PTQ/QDQ mixed precision

> **Ngày:** 2026-06-05
> **Thiết bị / Runtime:** ONNX Runtime / Qualcomm AI Hub
> **Model / Artifact nguồn:** `artifacts/deployment/exports/exported_model/vision_onnx`
> **Mục tiêu:** Tiếp tục thử Lite-MP/mixed precision để sửa QDQ fidelity của vision encoder trước khi compile/link QNN.
> **Trạng thái đầu ngày:** FOLLOW-UP

---

## 1. Phạm vi file này

File này ghi kết quả chạy trong ngày 2026-06-05 và checklist hiện hành cho ngày hôm nay.

Kết quả chi tiết của ngày 2026-06-04 nằm ở:

```text
deployment/docs/journal/[deploy]-2026-06-04.md
```

Không tick ngược file 2026-06-04. Nếu có run mới trong ngày 2026-06-05, ghi tiếp vào file này.

---

## 2. Trạng thái đầu ngày

### 2.1 Baseline/diagnostic đã khóa từ trước

- Static ONNX vs PyTorch trên `vn3k_test_10` đã pass: `cosine_l2_mean = 1.0000`.
- QDQ/QNN calib500 fail nặng, xác định lỗi chính nằm ở PTQ/QDQ trước runtime QNN.
- Raw input audit đã pass cho `vn3k_train_calib_500`, `vn3k_train_calib_2000`, và `vn3k_test_10`.
- Calibration dataset AI Hub đang dùng: `d7jzjy1m2`, từ `vn3k_train_calib_2000`.

### 2.2 Vai trò của các tập data

- `vn3k_train_calib_2000` / dataset ID `d7jzjy1m2`: dùng để AI Hub/AIMET calibrate quantization range khi tạo QDQ model.
- `vn3k_test_10`: dùng làm smoke fidelity gate độc lập sau quantize, so QDQ ONNX với PyTorch trên input không dùng để calibrate.
- Có thể compare thêm trên `vn3k_train_calib_2000` như diagnostic phụ, nhưng gate chính vẫn nên là tập độc lập.

### 2.3 QDQ candidates đã có trước ngày 2026-06-05

| Candidate | Config | AI Hub/AIMET PSNR | cosine_l2_mean | cosine_l2_min | cosine_l2_max | Gate | Quyết định |
|---|---|---:|---:|---:|---:|---|---|
| `jgomex415` | W8A8 calib2000 | `17.9452` | `0.1692` | `0.1239` | `0.1962` | FAIL | Không compile/link |
| `jp2j31dm5` | W8A16 calib2000 | `17.3564` | `0.1863` | `0.1366` | `0.2478` | FAIL | Không compile/link |
| `j5m4vjxd5` | W8A8 calib2000 + `min_max` | `17.8713` | `0.1658` | `0.1218` | `0.2159` | FAIL | Không compile/link |
| `jgl7en9l5` | Lite-MP default calib2000 | `18.6722` | `0.1906` | `0.1784` | `0.2212` | FAIL | Không compile/link |

Kết luận đầu ngày: Lite-MP default đang tốt nhất trong các candidate đã thử, nhưng vẫn thấp rất xa QDQ gate `cosine_l2_mean >= 0.95` và `cosine_l2_min >= 0.90`.

---

## 3. Kết quả chạy hôm nay

Chưa có run mới trong ngày 2026-06-05.

Khi có run mới, thêm vào bảng:

| Candidate | Config | AI Hub/AIMET PSNR | cosine_l2_mean | cosine_l2_min | cosine_l2_max | Gate | Quyết định |
|---|---|---:|---:|---:|---:|---|---|
| TBD | TBD | TBD | TBD | TBD | TBD | TBD | TBD |

---

## 4. Checklist hiện hành

### Phase A - Artifact và logging

- [x] Dùng `d7jzjy1m2` làm calibration dataset ID cho calib2000.
- [x] Dùng `artifacts/deployment/runtime/ptq_experiments/` để lưu compare output.
- [ ] Với mỗi run 2026-06-05, lưu đủ:
  - log job
  - QDQ ONNX directory
  - `qdq_vs_pytorch_summary.json`
  - `qdq_vs_pytorch.csv`
  - pass/fail decision

### Phase B - Thử mixed precision tiếp theo

- [ ] Chạy Lite-MP 30% INT16, quantize-only.
- [ ] Tải QDQ ONNX của Lite-MP 30% INT16 về local.
- [ ] Chạy QDQ-vs-PyTorch trên `vn3k_test_10`.
- [ ] Nếu Lite-MP 30% INT16 vẫn fail, chạy Lite-MP 10% FP16.
- [ ] Với mỗi candidate, không compile/link nếu QDQ gate fail.

### Phase C - Nếu AI Hub options vẫn fail

- [ ] Xem xét compare thêm trên `vn3k_train_calib_2000` để biết fail có xảy ra ngay trên calibration set không.
- [ ] Nghiên cứu pipeline quantization khác có khả năng exclude/mixed precision theo node/op.
- [ ] Ưu tiên giữ float cho các vùng nhạy cảm nếu toolchain cho phép:
  - LayerNorm
  - projection/head cuối
  - L2-normalization path
  - attention/softmax score path

### Phase D - Compile/link QNN

- [ ] BLOCKED: chỉ compile/link khi QDQ đạt `cosine_l2_mean >= 0.95` và `cosine_l2_min >= 0.90`.
- [ ] BLOCKED: chạy RB3 `vn3k_test_10` bằng `qnn-net-run`.
- [ ] BLOCKED: sync output về local và chạy QNN-vs-PyTorch compare.
- [ ] BLOCKED: mở rộng sang `vn3k_test_100` và full VN3K retrieval/R@1.

### Phase E - Text encoder và end-to-end

- [ ] BLOCKED: compile text encoder sau khi vision pass.
- [ ] BLOCKED: đo end-to-end image/text retrieval trên board.
- [ ] BLOCKED: cập nhật benchmark report và deploy guide.

---

## 5. Command cho việc tiếp theo

### Lite-MP 30% INT16

```bash
venv/bin/python deployment/scripts/qnn/submit_qaihub_quantize_compile.py \
  --model artifacts/deployment/exports/exported_model/vision_onnx \
  --calibration-data d7jzjy1m2 \
  --weights-dtype int8 \
  --activations-dtype int8 \
  --quantize-options="--lite_mp percentage=30;override_qtype=int16" \
  --name msiglip-vision-lite-mp-30-int16-calib2000-qonly \
  --wait \
  --quantize-only \
  --download-quantized artifacts/deployment/runtime/ptq_experiments/calib2000_lite_mp_30_int16/qdq_onnx
```

### Lite-MP 10% FP16

Chỉ chạy nếu Lite-MP 30% INT16 vẫn fail.

```bash
venv/bin/python deployment/scripts/qnn/submit_qaihub_quantize_compile.py \
  --model artifacts/deployment/exports/exported_model/vision_onnx \
  --calibration-data d7jzjy1m2 \
  --weights-dtype int8 \
  --activations-dtype int8 \
  --quantize-options="--lite_mp percentage=10;override_qtype=fp16" \
  --name msiglip-vision-lite-mp-10-fp16-calib2000-qonly \
  --wait \
  --quantize-only \
  --download-quantized artifacts/deployment/runtime/ptq_experiments/calib2000_lite_mp_10_fp16/qdq_onnx
```

### Compare QDQ mới

```bash
venv/bin/python deployment/scripts/qnn/compare_onnx_with_pytorch.py \
  --onnx-model artifacts/deployment/runtime/<QDQ_ONNX_DIR> \
  --model-dir artifacts/deployment/exports/exported_model \
  --input-dir artifacts/deployment/qnn_inputs/vn3k_test_10 \
  --precision fp32 \
  --json artifacts/deployment/runtime/ptq_experiments/<EXP>/qdq_vs_pytorch_summary.json \
  --csv artifacts/deployment/runtime/ptq_experiments/<EXP>/qdq_vs_pytorch.csv
```

### Diagnostic phụ trên calibration set

Chỉ dùng để hiểu thêm lỗi, không thay thế gate độc lập.

```bash
venv/bin/python deployment/scripts/qnn/compare_onnx_with_pytorch.py \
  --onnx-model artifacts/deployment/runtime/<QDQ_ONNX_DIR> \
  --model-dir artifacts/deployment/exports/exported_model \
  --input-dir artifacts/deployment/qnn_inputs/vn3k_train_calib_2000 \
  --precision fp32 \
  --json artifacts/deployment/runtime/ptq_experiments/<EXP>/qdq_vs_pytorch_calib2000_summary.json \
  --csv artifacts/deployment/runtime/ptq_experiments/<EXP>/qdq_vs_pytorch_calib2000.csv
```

---

## 6. Câu hỏi mở / rủi ro

- Lite-MP có thể vẫn không đủ nếu phần nhạy cảm nằm ở LayerNorm/projection/L2-normalization mà AI Hub option hiện tại không exclude đúng node.
- PSNR của AI Hub/AIMET không thay thế cosine embedding fidelity; mỗi candidate vẫn phải chạy QDQ-vs-PyTorch.
- Nếu tất cả candidate AI Hub quantize-only fail quanh `0.16-0.20`, cần chuyển sang hướng quantization pipeline có node-level control.
